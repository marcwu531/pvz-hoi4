#ifndef LEVEL_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define LEVEL_H

extern std::unordered_map<int, std::unordered_map<int, std::vector<int>>> world_level_zombies;

#endif
